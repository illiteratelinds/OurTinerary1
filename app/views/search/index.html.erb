<% if @results.empty? %>
  <%= "No results matching that query." %>
  <% else %>
    <ul>
      <% @results.each do |result| %>
        <li><%= link_to result.name, result %></li>
    <% end %>
    </ul>
<% end %>