<h2>User's Itineraries</h2><br><br>


<div class="center">
<% @users.each do |user| %>
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <%= image_tag user.avatar.url(:medium) %>
        <div class="caption">
          <h3><%= link_to user.name, user_path(user) %></h3>
          <% user.itineraries.each do |i| %>
            <li><%= link_to i.title, itinerary_path(i) %></li>
          <% end %><br>
        </div>
    </div>
  </div>
<%end %>